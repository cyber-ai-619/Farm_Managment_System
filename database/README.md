# Database

This folder is reserved for the database layer of the assignment.

Suggested contents as the project grows:

- `docs/backend/schema.md` for the table plan and implemented SQL schemas
- `migrations/` for versioned schema changes
- `seeders/` for sample data
- `init/` for Docker initialization scripts

